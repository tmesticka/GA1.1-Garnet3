functions:
Add(a,b): adds two numbers
subtract(a,b): subtracts b from a
multiply(a,b): takes the 2 numbers and multiplies them
sum_list(values): takes a array of numbers and adds them together
distance_from_zero(c): takes number and checks if its lower then zero, if true it uses subtract(0, c) 
square(x): uses multiply function to square x
